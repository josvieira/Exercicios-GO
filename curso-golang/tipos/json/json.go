package main

//converter struct para json
//json, error := json.Marshal(struct)//retorna um slice de bytes precisa converter para string
//json2 := string(json)

//converter um json para struct
//json.Unmarshal([]byte(jsonString), &nomestruct)
